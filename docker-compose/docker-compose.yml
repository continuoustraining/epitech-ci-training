version: "3"

networks:
  private:
    driver : bridge

services:
  mysql:
    image: mysql:5.6
    expose:
      - 3306
    ports:
      - "3306:3306"
    environment:
      MYSQL_ALLOW_EMPTY_PASSWORD: "yes"
      MYSQL_DATABASE: "my_db"
      MYSQL_USER: "iskandre"
      MYSQL_PASSWORD: "changeme"
    networks:
      - private
  worker:
    image: myimage:1.0
    ports:
      - "8080:8080"
    networks:
      - private
